#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int> x, y;
    int n, m, ans;
    int i;

    cin >> n >> m;

    x.resize(m);
    y.resize(m);
    
    for (i = 0; i < m; i++) cin >> x.at(i) >> y.at(i);

    ans = 0;
    

    return 0;
}