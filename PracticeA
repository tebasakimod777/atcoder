#include "stdio.h"

int main() {
    int a, b, c;
    char s[101];

    // 一行目入力
    scanf("%d", &a);
    // 二行目入力
    scanf("%d %d", &b, &c);
    // 三行目入力
    scanf("%s", s);
    printf("%d %s\n", a + b + c, s);

    return 0;
}
