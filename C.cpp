#include <bits/stdc++.h>
using namespace std;

int main() {

    int n, a, b, c;
    vector<int> l;

    cin >> n >> a >> b >> c;
    l.resize(n);
    
    for(int i = 0; i < n; i++)
        cin >> l.at(i);

    

    return 0;
}