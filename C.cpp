#include <bits/stdc++.h>
#define f(i, n) for(int i = 0; i < n; i++)
using namespace std;

typedef long long ll;

int main() {
    ll n, q, sur = 0;
    char s, t;
    string s;

    cin >> n >> q >> s;

    f(i, q) {
        cin >> s >> t;

    }

    puts(sur);

    return 0;
}