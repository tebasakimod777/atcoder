#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, k, i;
    cin >> n >> k;

    vector<int> input_a(n);

    for (i = 0; i < input_a.size(); i++)
        cin >> input_a.at(i);

    i = 0;
    while(dist != true) {

        if(){
            dist = true;
        }
    }

    return 0;
}
