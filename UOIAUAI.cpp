#include <bits/stdc++.h>
using namespace std;

int main() {
    char ch;
    cin >> ch;

    if ( ch == 'a' || ch == 'i' || ch == 'u' || ch == 'e' || ch == 'o') {
        cout << "vowel" << endl;
    } else {
        cout << "consonant" << endl;
    }

    return 0;
}
